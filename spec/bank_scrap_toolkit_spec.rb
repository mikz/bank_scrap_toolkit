require 'spec_helper'

describe BankScrapToolkit do
  it 'has a version number' do
    expect(BankScrapToolkit::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
